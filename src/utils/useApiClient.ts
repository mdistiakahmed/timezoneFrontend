export type GetParams = {
    page: number;
};

export const useApiClient = (dispatch: Dispatch, axiosClient: AxioHandler) => {
    // get

    const getData = (url: string, params: GetParams) => {
        //axiosClient
    };

    // post
};
